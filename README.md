# Run
You have to put the dataset in `./speechesdataset` folder first. The shell script to run the code is `bash run.sh`.

The logs are saved in `./log` folder

The attention maps are saved in `attn_maps` folder.